let arr = [1,2,3,4,5,6,7];
arr.reduce((akk, curr) => akk +curr,0);
console.log(arr);
let newArr = arr.reduce((akk, curr) => akk +curr,0);
console.log(newArr);
console.log(arr.reduce((akk, curr) => akk +curr,0));
